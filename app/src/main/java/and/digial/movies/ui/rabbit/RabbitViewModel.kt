package and.digial.movies.ui.rabbit

import and.digial.network.MoviesRepo
import androidx.lifecycle.ViewModel

class RabbitViewModel(private val repo: MoviesRepo): ViewModel() {

    fun getRecommendations(movieId: Int) {

    }
}